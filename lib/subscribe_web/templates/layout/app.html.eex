<!DOCTYPE html>
<html lang="en" prefix="og: http://ogp.me/ns#">
  <head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1">

    <link rel="apple-touch-icon" sizes="180x180" href="<%= static_path(@conn, "/favicon/apple-touch-icon.png") %>">
    <link rel="icon" type="image/png" sizes="32x32" href="<%= static_path(@conn, "/favicon/favicon-32x32.png") %>">
    <link rel="icon" type="image/png" sizes="16x16" href="<%= static_path(@conn, "/favicon/favicon-16x16.png") %>">
    <link rel="manifest" href="<%= static_path(@conn, "/favicon/site.webmanifest") %>">
    <link rel="mask-icon" href="<%= static_path(@conn, "/favicon/safari-pinned-tab.svg") %>" color="#ffffff">
    <meta name="msapplication-TileColor" content="#2d89ef">
    <meta name="theme-color" content="#ffffff">

    <%= render_existing(@view_module, "meta." <> @view_template, assigns) %>

    <title>Podlove | Subscribe</title>
    <link rel="stylesheet" href="<%= static_path(@conn, "/css/app.css") %>">
  </head>

  <body class="sans-serif bg-near-black">

    <!--
    <p class="alert alert-info" role="alert"><%= get_flash(@conn, :info) %></p>
    <p class="alert alert-danger" role="alert"><%= get_flash(@conn, :error) %></p>
    -->

    <main role="main">
      <%= render @view_module, @view_template, assigns %>
    </main>

    <!-- 
    <script src="<%= static_path(@conn, "/js/app.js") %>"></script>
    -->
  </body>
</html>
